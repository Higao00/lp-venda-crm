import styled from "styled-components"

export const Testimonials = styled.section`
    padding: 60px 20px;
    background: #f1f1f1;
    text-align: center;
    blockquote {
        font-style: italic;
        max-width: 600px;
        margin: auto;
        cite {
            display: block;
            margin-top: 10px;
            font-weight: bold;
        }
    }
`
